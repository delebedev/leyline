package leyline.bridge.types

/** Zone category at semantic reveal completion, before wire-zone projection. */
enum class RevealZone {
    HAND,
    LIBRARY,
    SIDEBOARD,
    GRAVEYARD,
    BATTLEFIELD,
    EXILE,
    COMMAND,
    STACK,
}
