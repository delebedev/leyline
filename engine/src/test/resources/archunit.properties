# A rule whose `that(...)` clause matches nothing passes by default, so a
# renamed class silently retires the boundary it was pinning. The suites in
# leyline.architecture are table-driven, where that failure mode is invisible
# on review — make an empty match an error instead.
archRule.failOnEmptyShould=true
