# IDE
*.idea
*.iml
*.tmp
.metadata
.recommenders
.settings
.classpath
.project
.checkstyle
.vscode/settings.json
.vscode/launch.json
.factorypath
nbactions.xml

# Gradle
.gradle/
**/build/
target

# Logs
*.log
/logs/

# Player data (created by `just seed-db`)
/data/
!/data/decks/

# Card database files (read from user's MTGA install at runtime)
*.mtga

# macOS
.DS_Store

# Vim
*.swp

# Kotlin compiler daemon
.kotlin/

# Python
__pycache__
*.pyc

# Local automation/browser artifacts
/output/
kls_database.db
.playwright-mcp/
.playwright-screenshots/

# Per-worktree Maven local repo
.m2-local/

# Forge submodule install stamp (written by `just install-forge`)
.upstream-installed

# Claude Code local settings & agent memory
**/settings.local.json
.claude/agent-memory/

# Persistent game recordings (runtime artifacts from proxy sessions)
/recordings/

# Generated proto (from submodule + rename-map.sed)
app/main/proto/messages.proto
/proto-gen/

# Forge submodule build artifacts handled by its own .gitignore
# Local-only dev notes
.local/

!.claude/
.claude/CLAUDE.md
.claude/worktrees/

# Contains creds
deploy/services-proxy.conf

# Wrangler local state
.wrangler/

# Engine debug captures
matchdoor/engine/*.bin
matchdoor/engine/*.txt

# ML models (large, regenerable from leyline-vision)
*.mlmodel

# Autoplay session videos (large, ephemeral)
docs/retro/autoplay/**/gameplay.mp4
PROGRESS.md
RALPH.md

# Local issue sync (gh-issue-sync)
.issues/
node_modules/

# Generated changelog (build artifact from scripts/curate-changelog.ts)
.changelog.md
launcher/.changelog.md
.gitlog

# Launcher (Tauri/Rust build artifacts)
launcher/src-tauri/target/
launcher/dist/
launcher/bun.lockb

# Proof-loop task artifacts (ephemeral, per-session)
.agent/tasks/

# Beads / Dolt files (added by bd init)
.dolt/
*.db
.beads-credential-key
