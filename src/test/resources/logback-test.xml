<configuration>
  <statusListener class="ch.qos.logback.core.status.NopStatusListener"/>

  <appender name="STDOUT" class="ch.qos.logback.core.ConsoleAppender">
    <encoder>
      <pattern>%-5level %logger{36} - %msg%n</pattern>
    </encoder>
  </appender>

  <!-- Bridge timeouts are expected in tests (auto-pass on unhandled phases).
       Suppress the 15-line stack-trace diagnostic that fires per timeout. -->
  <logger name="forge.nexus.bridge.GameActionBridge" level="ERROR"/>
  <logger name="forge.nexus.bridge.InteractivePromptBridge" level="ERROR"/>
  <logger name="forge.nexus.bridge.MulliganBridge" level="ERROR"/>
  <logger name="forge.nexus.bridge.BridgeTimeoutDiagnostic" level="ERROR"/>

  <!-- Only WARN+ during tests — keeps gate output clean -->
  <root level="WARN">
    <appender-ref ref="STDOUT"/>
  </root>
</configuration>
