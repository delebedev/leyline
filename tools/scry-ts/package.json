{
  "name": "scry",
  "version": "0.0.1",
  "description": "MTGA Player.log parser and game state tool",
  "type": "module",
  "bin": {
    "scry": "./cli.ts"
  },
  "scripts": {
    "scry": "bun run cli.ts",
    "test": "bun test",
    "check": "tsc --noEmit && bun test"
  },
  "devDependencies": {
    "@types/bun": "^1.3.11",
    "@types/node": "^25.5.2"
  }
}
