<configuration>
  <statusListener class="ch.qos.logback.core.status.NopStatusListener"/>

  <appender name="STDOUT" class="ch.qos.logback.core.ConsoleAppender">
    <encoder>
      <pattern>%-5level %logger{36} - %msg %kvp%n</pattern>
    </encoder>
  </appender>

  <!-- Bridge timeouts are expected in tests (auto-pass on unhandled phases).
       Suppress the 15-line stack-trace diagnostic that fires per timeout. -->
  <logger name="leyline.bridge.handoff.GameActionBridge" level="ERROR"/>
  <logger name="leyline.bridge.handoff.InteractivePromptBridge" level="ERROR"/>
  <logger name="leyline.bridge.handoff.MulliganBridge" level="ERROR"/>
  <logger name="leyline.bridge.BridgeTimeoutDiagnostic" level="ERROR"/>

  <!-- Only WARN+ during tests — keeps gate output clean -->
  <root level="WARN">
    <appender-ref ref="STDOUT"/>
  </root>
</configuration>
