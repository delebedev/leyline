<?xml version="1.0" ?>
<SmellBaseline>
  <ManuallySuppressedIssues></ManuallySuppressedIssues>
  <CurrentIssues>
    <ID>ImplicitDefaultLocale:CaptureSink.kt$String.format("%02x", ft)</ID>
    <ID>LongMethod:LeylineServer.kt$LeylineServer$private fun startLocal(fdSsl: SslContext, mdSsl: SslContext)</ID>
    <ID>LongParameterList:LeylineServer.kt$LeylineServer$( private val frontDoorPort: Int = 30010, private val matchDoorPort: Int = 30003, /** TLS cert+key (PEM). Falls back to self-signed if null. Needed when client validates certs (UnityTls). */ private val certFile: java.io.File? = null, private val keyFile: java.io.File? = null, /** Proxy mode: if set, relay to these upstream IPs instead of stubbing. */ private val upstreamFrontDoor: String? = null, private val upstreamMatchDoor: String? = null, /** FD golden file: if set, use replay-based FD stub instead of hand-crafted. */ val fdGoldenFile: File? = null, /** Playtest configuration (decks, seed, die roll, AI speed). */ val matchConfig: MatchConfig = MatchConfig(), /** Puzzle mode: if set, load this .pzl file for all client connections. */ val puzzleFile: File? = null, /** External hostname for MatchCreated push (client connects here for MD). Defaults to localhost. */ private val externalHost: String = "localhost", /** Card data repository — passed to MatchHandler for grpId↔name lookups. */ private val cardRepo: leyline.game.CardRepository, /** Resolved player database file — must exist and contain a seeded player (run `just seed-db`). */ private val playerDbFile: File, )</ID>
    <ID>UnusedParameter:LeylineMain.kt$a: Map&lt;String, String&gt;</ID>
    <ID>UnusedParameter:LeylineServer.kt$LeylineServer$name: String</ID>
  </CurrentIssues>
</SmellBaseline>
