# Changelog

Domain-grouped. Maintained per-PR — add to `[Unreleased]`, finalized at release.

## [Unreleased]

### Gameplay

### Launcher

### Fixed

### Developer
- scry-ts: new `inspect` command (accumulated permanent state), ability resolution on stack, persistent annotations in `gsm show`, pending prompts + mana costs in `board`, colored output (chalk), protocol types (`RawGameStateMessage`), golden tests against reference game, `bun run check` (tsc strict + tests), shared `formatAnnotations`/`formatManaCost` helpers, README/CLAUDE.md dedup. 7 beads closed.
- Documentation strategy overhaul: `read_when` frontmatter on 45+ docs, `just docs [filter]` recipe, expanded principles, CONTRIBUTING beefed up, stale plans purged and compressed into `wire-spec-archive.md`
