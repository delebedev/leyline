# Forge Upstream Sync Runbook

**Moved to skill:** `.claude/skills/forge-upstream-sync/SKILL.md`

Invoke via `/forge-upstream-sync` or let Claude auto-detect when you ask to sync the forge submodule.

The skill contains the full procedure, fork-local delta inventory, troubleshooting, and validation history.
